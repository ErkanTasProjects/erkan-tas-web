const fs = require('fs'); const { JSDOM } = require('jsdom'); const html = fs.readFileSync('C:\Users\HezaPcKolog\Desktop\ERKAN TAÞ WEB SÝTE\erkan-tas-website\index.html', 'utf8'); const dom = new JSDOM(html, { runScripts: 'dangerously', resources: 'usable' }); dom.window.onerror = function(msg, file, line, col, error) { console.error('JSDOM ERROR:', msg, error); };setTimeout(() => console.log('Done'), 2000);
